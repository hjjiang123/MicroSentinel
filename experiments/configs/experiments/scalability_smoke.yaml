suite: scalability_smoke
workload: kv
config: experiments/configs/workloads/kv.yaml
run_s: 20
warmup_s: 5
repetitions: 1
modes:
  - baseline
overrides:
  annotations:
    suite: scalability_smoke
parameters:
  rate_scan:
    samples_per_core: [1000]
    event_sets:
      - [LLC-load-misses]
  filters:
    - mode: full
