suite: flow_accuracy_smoke
run_s: 15
warmup_s: 0
repetitions: 1
modes: [microsentinel]
overrides:
  annotations:
    suite: flow_accuracy_smoke
workloads:
  - name: load_balancer
    config: experiments/configs/workloads/load_balancer.yaml
    annotations:
      scenario: flow_ground_truth
    parameters:
      delta_values_us: [10]
      client_variants:
        - name: tenant-a
          flows: 64
    overrides:
      lb_node:
        # Use the default Python load balancer to avoid haproxy dependency.
      clients:
        implementation: builtin
        remote:
          host: "211.65.193.243"
          workdir: "/home/hjjiang/MicroSentinel"
          metrics_dir: "/home/hjjiang/MicroSentinel/artifacts/remote"
        generator: python3 experiments/workloads/lb/lb_client.py
        flows: 64
        ground_truth_log: truth/lb_ground_truth.json
