suite: false_sharing
run_s: 180
warmup_s: 30
workload: load_balancer
config: experiments/configs/workloads/load_balancer.yaml
modes:
  - microsentinel
  - baseline
overrides:
  annotations:
    suite: false_sharing
parameters:
  pmu_events:
    - [OFFCORE_RESPONSE.DEMAND_RFO.HITM, MEM_LOAD_L3_MISS.REMOTE_DRAM]
